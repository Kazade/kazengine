#include <cassert>

#include "plane.h"

Plane::Plane() {
	assert(0);
}

Plane::Plane(const kmVec3& v1, const kmVec3& v2, const kmVec3& v3) {
	assert(0);
}

Plane::Plane(const Plane& p) {
	assert(0);
}
