#ifndef IGUIENVIRONMENT_H_INCLUDED
#define IGUIENVIRONMENT_H_INCLUDED

#include <kazmathxx/vec2.h>
/*#include <openglui/Widget.h>

#include <tr1/memory>
#include <vector>

#include "ieventhandler.h"

using std::tr1::shared_ptr;
using std::vector;

class GuiEnvironment : public IEventHandler {
	public:
		GuiEnvironment(const string& layout);
		virtual ~GuiEnvironment();

		void addWidget(shared_ptr<ui::Widget> newWidget);
		void removeWidget(shared_ptr<ui::Widget> widget);

	private:
		vector<shared_ptr<ui::Widget> > m_Widgets;
};*/

#endif // IGUIENVIRONMENT_H_INCLUDED
